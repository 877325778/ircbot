<?php
/*
 * 数据库链接脚本
 *
 * 功能说明：
 * 	包含本文件后，会自动联接数据库
 */
$dbhost = "localhost";
$dbuser = "username";
$dbpass = "passwd";
global $dbname;
$dbname = "dbname";

mysql_connect($dbhost, $dbuser, $dbpass) || die("无法联接服务器");
mysql_select_db($dbname) || die("数据库不存在");

?>
